
/*

class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
        // int n = nums.size();
        vector<int>ans;
        for(auto it:nums)
        {
            int x = (it) * (it);
            ans.push_back(x);
        }
        sort(ans.begin(),ans.end());
        return ans;
    }
};

*/